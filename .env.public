# =====================================================
# ACE CSS LEAVE PORTAL - PUBLIC HOSTING CONFIGURATION
# =====================================================
# IMPORTANT: Replace YOUR_PUBLIC_IP with your actual public IP address
# Example: If your IP is 203.0.113.45, replace YOUR_PUBLIC_IP with 203.0.113.45

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Ace_cs@2025
DB_NAME=cyber_security_leave_portal
DB_PORT=3307

# JWT Configuration - CHANGE THIS TO A STRONG SECRET
JWT_SECRET=your_super_strong_jwt_secret_key_for_production_2025

# Server Configuration
PORT=3009
NODE_ENV=production

# Public IP Configuration (REPLACE WITH YOUR ACTUAL PUBLIC IP)
PUBLIC_IP=YOUR_PUBLIC_IP
DOMAIN=YOUR_PUBLIC_IP

# Frontend Configuration for Production
VITE_API_URL=http://YOUR_PUBLIC_IP:3009

# CORS Origins - Add your domain/IP
CORS_ORIGIN=http://YOUR_PUBLIC_IP:8085,http://YOUR_PUBLIC_IP:3000,http://localhost:8085,http://localhost:3000

# Security Settings
SECURE_COOKIES=false
TRUST_PROXY=true

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=adhiyamaancyber@gmail.com
SMTP_PASS=clvl ovxg iocd atan
SMTP_FROM=adhiyamaancyber@gmail.com

# File Upload Settings
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/uploads

# Session Settings
SESSION_TIMEOUT=3600000

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
